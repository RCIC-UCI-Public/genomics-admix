!include bbmap.yaml
!include rcic-module.yaml
---
- package: bbmap Module
  category: GENOMICS
  module:
    setenv: 
      - BBMAP_HOME {{root}}
    prepend_path:
      - PATH {{root}}
      - CLASSPATH {{root}}/current
