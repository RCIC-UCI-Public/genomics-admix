# Builds the module for plinkseq.
!include plinkseq.yaml
!include rcic-module.yaml
---
- package: plinkseq Module
  category: GENOMICS
  module:
    requires:
      - *Requires
      - "{{build.modules}}"

